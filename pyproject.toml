[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "le-wm-minecraft"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "numpy",
    "cv2",
    "matplotlib",
]
